export enum ReputeTypeEnum {
    SubmissionAccepted = "submission_accepted",
    SubmissionRejected = "submission_rejected",
    SubmissionRewarded = "submission_rewarded",
    Abuse = "abuse",
}

export enum ReputeTargetTypeEnum {
    Submission = "submission",
}